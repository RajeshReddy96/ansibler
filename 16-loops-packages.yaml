- name: loops
  hosts: web
  become: yes
  tasks:
  - name: print names
    ansible.builtin.package:
      msg: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - postfix
    - httpd